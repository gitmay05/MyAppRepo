@model AllinOneApiApplication.Model.Form.FormModel

@{
    ViewData["Title"] = "AddEditForm";
    ViewBag.PageName = "Form";

}
@* 
@{
    string Userstring = Context.Session.GetString("UserInfo");
    UserModel userInfo = JsonConvert.DeserializeObject<UserModel>(Userstring);
}
 *@
<h1>Add/EditForm</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddEditForm">
            @* <input type="hidden" asp-for="CreatedBy" value="@userInfo.UserId" class="form-control" /> *@
            <input type="hidden" asp-for="PK_FormId" value="@Model.PK_FormId" class="form-control" />


            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
           @*  <div class="form-group">
                <label class="control-label">Parent</label>
                <select asp-for="FK_ParentId" asp-items="@(new SelectList(@ViewBag.FormList,"ID","Value"))" class="form-control">
                    <option value="0">--Select--</option>
                </select>

                <span asp-validation-for="FK_ParentId" class="text-danger"></span>
            </div> *@
            <div class="form-group">
                <label class="control-label">Form Name</label>
                <input asp-for="FormName" class="form-control" placeholder="Form Name" />
                <span asp-validation-for="FormName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Controller Name</label>
                <input asp-for="ControllerName" class="form-control" placeholder="Controller Name" />
                <span asp-validation-for="ControllerName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Action Name</label>
                <input asp-for="ActionName" class="form-control" placeholder="Action Name" />
                <span asp-validation-for="ActionName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Class Name</label>
                <input asp-for="ClassName" class="form-control" placeholder="Class Name" />
                <span asp-validation-for="ClassName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Area</label>
                <input asp-for="Area" class="form-control" placeholder="Area" />
                <span asp-validation-for="Area" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label class="control-label">Status</label>
                <select asp-for="Status" class="form-control" asp-items="new SelectList(ViewBag.Status)"></select>
            </div>
            <br />
            @*<div class="form-group">
            <input type="submit" value="Submit" class="btn btn-primary" />
            </div>*@
            <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-primary" />
                <input type="submit" value="cancel" class="btn btn-primary" asp-action="Index" />
            </div>
        </form>
    </div>
</div>
@section Scripts{
  @*   <link rel="stylesheet" href=" https://cdn.datatables.net/1.13.1/css/dataTables.bootstrap5.min.css" />
    <script type="text/javascript" src="https://cdn.datatables.net/1.13.1/js/jquery.dataTables.min.js"></script>
 *@

    <script type="text/javascript">
        $(function () {


            $.ajax({
                type: "GET",
                url: "@Url.Action("GetFormDetails", "Form")",
                // data: { "customerId": customerId },
                success: function (data) {
                    $("#GridDiv").html(data);
                },
                failure: function (response) {
                    alert(response.responseText);
                },
                error: function (response) {
                    alert(response.responseText);
                }
            });


        });
    </script>
}

