@model AllinOneApiApplication.Model.User.UserModel

@{
    ViewData["Title"] = "AddEditUser";

}
@* 
@{
    string Userstring = Context.Session.GetString("UserInfo");
    UserModel userInfo = JsonConvert.DeserializeObject<UserModel>(Userstring);
} *@

<h1>Add /Edit User</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddEditUser">
           @*  <input type="hidden" asp-for="SessionUserId" value="@userInfo.UserId" class="form-control" /> *@
            <input type="hidden" asp-for="UserId" value="@Model.UserId" class="form-control" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label class="control-label">Account</label>
                <select asp-for="FK_AccountId" asp-items="@(new SelectList(@ViewBag.AccountList,"Id","Value"))" class="form-control">
                    <option value="0">--Select--</option>
                </select>
                <span asp-validation-for="FK_AccountId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label class="control-label">Role</label>
                <select asp-for="FK_RoleId" asp-items="@(new SelectList(@ViewBag.RoleList,"Id","Value"))" class="form-control">
                    <option value="0">--Select--</option>
                </select>
                <span asp-validation-for="FK_RoleId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label class="control-label">User Name</label>
                <input asp-for="UserName" class="form-control" placeholder="User Name" />
                <span asp-validation-for="UserName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Password</label>
                <input asp-for="Password" class="form-control" placeholder="Password" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label class="control-label">Email</label>
                <input asp-for="Email" class="form-control" placeholder="Email" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label class="control-label">Status</label>
                <select asp-for="Status" class="form-control" asp-items="new SelectList(ViewBag.Status)"></select>
            </div>


            @*<div class="form-group">
                    <label asp-for="ImageName" class="control-label"></label>
                    <input asp-for="ImageName" class="form-control" />
                    <span asp-validation-for="ImageName" class="text-danger"></span>
                </div>*@

        <div class="form-group">
            <input type="submit" value="Submit" class="btn btn-primary" />
            <input type="submit" value="cancel" class="btn btn-primary" asp-action="Index" />
        </div>
        </form>
    </div>
</div>



